name: Diagnostico (prueba temporal)

on:
  workflow_dispatch:

jobs:
  prueba:
    runs-on: ubuntu-latest
    steps:
      - name: Saludo
        run: echo "Si ves esto, los runners de GitHub funcionan bien para esta cuenta"
